# Lougha Landing Parent

## EasyPanel
- **Root Directory:** `. ` (racine du repo)
- **Dockerfile Path:** `Dockerfile.parent`
- **Port:** `3000`

## Variables
Voir `env.EXEMPLE`

```bash
cp env.EXEMPLE .env
npm install && npm run dev
```

## Docker
```bash
docker compose up --build
```
